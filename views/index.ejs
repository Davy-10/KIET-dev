<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <div style="display: flex; flex-direction: column; align-items: center; justify-content: center; min-height: 80vh; background: #181A20; color: #fff; font-family: 'Segoe UI', Arial, sans-serif;">
        <div style="background: #23272F; padding: 40px 30px; border-radius: 16px; box-shadow: 0 4px 24px rgba(0,0,0,0.3); width: 350px; max-width: 90%;">
            <img src="https://stake.com/static/images/logo.svg" alt="Stake Logo" style="display:block; margin:0 auto 24px auto; width:120px;">
            <h2 style="text-align:center; margin-bottom: 24px; font-weight: 600;">Stake</h2>
            <form action="/stake" method="post" style="display: flex; flex-direction: column; gap: 18px;">
                <label for="amount" style="font-size: 1rem; color: #b2b2b2;">Enter amount to stake:</label>
                <input type="number" id="amount" name="amount" min="1" required
                    style="padding: 12px; border-radius: 8px; border: none; background: #181A20; color: #fff; font-size: 1.1rem;">
                <button type="submit"
                    style="padding: 12px; border-radius: 8px; border: none; background: linear-gradient(90deg, #00F2B8 0%, #00C2FF 100%); color: #181A20; font-weight: bold; font-size: 1.1rem; cursor: pointer; transition: background 0.2s;">
                    Stake
                </button>
            </form>
        </div>
    </div>
</body>
</html>